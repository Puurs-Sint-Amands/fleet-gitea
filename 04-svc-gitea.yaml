apiVersion: v1
kind: Service
metadata:
  name: gitea-service
  namespace: gitea
spec:
  selector:
    workload.user.cattle.io/workloadselector: apps.deployment-gitea-gitea
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
